"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Any = void 0;
const decoratorSchemaFactory_1 = require("../utils/decoratorSchemaFactory");
const getJsonType_1 = require("../utils/getJsonType");
/**
 * Set the type of the array items.
 *
 * ::: warning
 * For v6 user, use @@Any@@ from @tsed/schema instead of @tsed/common.
 * :::
 *
 * ## Example
 *
 * ```typescript
 * class Model {
 *    @Any()
 *    property: any;
 * }
 * ```
 *
 * Will produce:
 *
 * ```json
 * {
 *   "type": "object",
 *   "properties": {
 *     "property": {
 *       "type": ["integer", "number", "string", "boolean", "array", "object", "null"]
 *     }
 *   }
 * }
 * ```
 *
 * @decorator
 * @validation
 * @swagger
 * @schema
 */
function Any(...types) {
    return decoratorSchemaFactory_1.decoratorSchemaFactory((schema) => {
        types = types.length ? types : ["integer", "number", "string", "boolean", "array", "object", "null"];
        types = types.map(getJsonType_1.getJsonType);
        schema.mapper.type = (types.length === 1 ? types[0] : types);
    });
}
exports.Any = Any;
//# sourceMappingURL=any.js.map