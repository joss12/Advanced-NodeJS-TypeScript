"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Title = void 0;
const core_1 = require("@tsed/core");
const schema_1 = require("./schema");
/**
 * Add title metadata on the decorated element.
 *
 * ::: warning
 * This decorator will be removed in v7.
 *
 * For v5 user, use @@Title@@ from @tsed/common instead of @tsed/swagger.
 *
 * For v6 user, use @@Title@@ from @tsed/schema instead of @tsed/common.
 * :::
 *
 * ## Example
 *
 * ```typescript
 * class Model {
 *    @Title("title")
 *    id: string;
 * }
 * ```
 *
 * Will produce:
 *
 * ```json
 * {
 *   "type": "object",
 *   "properties": {
 *     "id": {
 *        "type": "string",
 *        "title": "title"
 *     }
 *   }
 * }
 * ```
 *
 * @param {string} title
 * @decorator
 * @swagger
 * @schema
 * @classDecorator
 * @propertyDecorator
 * @input
 */
function Title(title) {
    return (...args) => {
        const type = core_1.decoratorTypeOf(args);
        switch (type) {
            case core_1.DecoratorTypes.METHOD:
                return core_1.StoreMerge("operation", { title })(...args);
            case core_1.DecoratorTypes.PARAM:
                return core_1.StoreMerge("baseParameter", { title })(...args);
            default:
                return schema_1.Schema({ title })(...args);
        }
    };
}
exports.Title = Title;
//# sourceMappingURL=title.js.map