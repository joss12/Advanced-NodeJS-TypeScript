import { IMiddleware, Req, Res, ResponseErrorObject } from "../../mvc";
import "../components/StringErrorFilter";
/**
 * Catch all errors and return the json error with the right status code when it's possible.
 *
 * ::: warning
 * Use @@Catch@@ decorator to handler a specific exception.
 * :::
 *
 * @middleware
 * @deprecated Use Catch decorator to handler a specific exception.
 */
export declare class GlobalErrorHandlerMiddleware implements IMiddleware {
    types: Map<import("@tsed/core").Type<any> | Symbol, import("..").ExceptionFilterMethods<unknown>>;
    protected headerName: string;
    use(error: any, request: Req, response: Res): any;
    setHeaders(response: Res, ...args: ResponseErrorObject[]): void;
}
