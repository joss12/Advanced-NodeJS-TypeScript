import { InjectorService } from "@tsed/di";
import { Req } from "../../mvc/decorators/params/request";
/**
 * Create a context with all required object to call next handlers.
 *
 * @platform
 * @middleware
 */
export declare class PlatformContextMiddleware {
    private injector;
    level: "debug" | "info" | "warn" | "error" | "off" | undefined;
    maxStackSize: number;
    ignoreUrlPatterns: string[];
    reqIdBuilder: any;
    constructor(injector: InjectorService);
    static onClose(err: any, response: TsED.Response): Promise<void>;
    use(req: Req, res: any, next: any): Promise<void>;
}
